# See docs at: https://mystmd.org/guide/frontmatter
version: 1
project:
  id: d58eaa1c-67af-451e-b63f-5caaa2041a78
  # title:
  # description:
  # keywords: []
  # authors: []
  # github:
  toc:
    - file: content/index.md
  plugins:
    - type: executable
      # .venv/pymd-plugin-bin.exe is generated by scripts/link_plugin_launcher.py, which must be
      # run after `uv sync` (see README.md "Setup"). It is not created by `uv sync` itself.
      path: .venv/pymd-plugin-bin.exe
site:
  template: book-theme
  options:
    favicon: favicon.ico
  #   logo: site_logo.png
