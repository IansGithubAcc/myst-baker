# See docs at: https://mystmd.org/guide/frontmatter
version: 1
project:
  id: d58eaa1c-67af-451e-b63f-5caaa2041a78
  # title:
  # description:
  # keywords: []
  # authors: []
  # github:
  toc:
    - file: content/index.md
    - title: Guide
      children:
        - file: content/inputs.md
        - file: content/calculations.md
        - file: content/outputs.md
        - file: content/gallery.md
  plugins:
    - type: executable
      # Generated directly by `uv sync` (see README.md "Setup"). MyST resolves this as a
      # literal path, not a PATH search, so it must match where uv puts the launcher.
      # Windows path shown below; on Linux/Mac use .venv/bin/pymd-plugin instead.
      path: .venv/Scripts/pymd-plugin.exe
site:
  template: book-theme
  options:
    favicon: favicon.ico
  #   logo: site_logo.png
